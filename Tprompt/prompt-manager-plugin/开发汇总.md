# 本地提示词管理插件 · 开发完成情况汇总

> 项目：`prompt-manager-plugin`（Chrome/Edge 浏览器扩展，Manifest V3）
> 汇总时间：2026-04-24
> 状态：**Phase 1 MVP + Phase 2 增强功能 + CSV/Markdown 导出 全部完成 ✅**

---

## 一、项目概览

| 维度 | 内容 |
|------|------|
| **项目名称** | Prompt Manager（提示词管理器插件） |
| **类型** | Chrome/Edge 浏览器扩展（MV3） |
| **技术栈** | Vue 3 + TypeScript + Vite + Pinia + Dexie.js + MiniSearch + vuedraggable |
| **存储方式** | IndexedDB（本地，零依赖） |
| **定位** | 纯本地、隐私友好的 AI 提示词管理工具 |

---

## 二、已完成功能清单

### ✅ Phase 1 — MVP 核心功能（100% 完成）

#### 2.1 数据层
- [x] **Dexie 数据库**（`src/db/schema.ts`）
  - 3 张表：`prompts` / `categories` / `settings`
  - 预置 5 个分类（写作助手、代码生成、数据分析、翻译助手、创意激发）
  - 支持 `sortOrder` 排序索引

#### 2.2 提示词 CRUD
- [x] **新建提示词**（标题、内容、描述、标签、分类）
- [x] **编辑提示词**（PromptEditor 组件，双模式编辑）
- [x] **删除提示词**（ConfirmDialog 二次确认）
- [x] **收藏功能**（星标置顶，虚拟分类联动）
- [x] **使用次数统计**（`usageCount`，复制时自动 +1）
- [x] **时间戳管理**（`createdAt` / `updatedAt` / `lastUsedAt`）

#### 2.3 分类管理
- [x] **内置虚拟分类**：全部、收藏
- [x] **自定义分类**：增删改、图标（emoji）/ 颜色自定义
- [x] **分类拖拽排序**（vuedraggable，动画 200ms）
- [x] **右键菜单操作**（编辑分类 / 删除分类）
- [x] **分类颜色选择器**（10 种预设颜色）
- [x] **分类图标选择器**（24 种 emoji 可选）

#### 2.4 搜索系统
- [x] **MiniSearch 全文搜索**（`src/composables/useSearch.ts`）
  - 索引字段：`title`（权重 3×）、`tagsText`（权重 2×）、`description`（权重 1.5×）、`content`（权重 1×）
  - 前缀匹配 + 模糊搜索（fuzzy 0.2）
  - 增量索引（新增 / 更新 / 删除单条同步）
- [x] **搜索历史**（localStorage，最多 10 条）
- [x] **搜索高亮**（标题 + 内容片段，双重高亮逻辑）
- [x] **空搜索状态**（分类空 / 无结果 / 无提示词 三种 EmptyState）

#### 2.5 导入 / 导出
- [x] **导出 JSON**（含版本号、导出时间戳）
  - 可选：包含分类信息 / 格式化 JSON
  - 实时预览（前 500 字符）
  - 文件名带日期：`prompts-backup-YYYY-MM-DD.json`
- [x] **导出 CSV**（表格格式，Excel 友好）
  - 包含字段：ID、标题、内容、描述、分类、标签、收藏、置顶、使用次数、创建时间、更新时间
  - 正确处理逗号、双引号、换行符
  - 分类自动映射为分类名称
- [x] **导出 Markdown**（阅读友好，笔记软件适用）
  - 按分类分组，H2 标题表示分类
  - 每个提示词为 H3 标题 + 代码块内容
  - 包含标签、使用次数等元信息
  - 支持 Obsidian、Notion 等笔记软件
- [x] **导出范围选择**（全部 / 已收藏）
- [x] **导入 JSON**（拖拽上传 + 点击选择）
  - 3 种冲突策略：合并（推荐）/ 替换（危险）/ 仅新增
  - 导入前解析预览（显示条数、分类数、版本）
  - 分类自动映射

#### 2.6 亮 / 暗模式
- [x] **主题切换**（亮色 / 暗色 / 跟随系统）
- [x] **CSS Variables** 方案（`--primary-*`, `--bg-*`, `--text-*` 等）
- [x] **ThemeToggle 组件**（Header 右侧一键切换）
- [x] **所有组件暗色适配**（背景、文字、边框、hover 态全覆盖）

---

### ✅ Phase 2 — 增强体验功能（100% 完成）

#### 2.7 分类拖拽排序
- [x] `vuedraggable` 集成（侧边栏自定义分类区域）
- [x] `ghost-class` + `chosen-class` 视觉反馈
- [x] 拖拽结束自动持久化 `sortOrder` 到 IndexedDB
- [x] Toast 确认提示"分类顺序已保存"

#### 2.8 Prompt 批量操作
- [x] **勾选模式**（卡片左上角复选框，hover 时显示）
- [x] **全选 / 取消全选**（工具栏按钮）
- [x] **批量操作栏**（选中后替换工具栏）
  - 批量收藏 / 取消收藏
  - 批量移动到分类（下拉菜单）
  - 批量删除（带确认）
- [x] **选中态视觉反馈**（主色边框 + 浅背景）

#### 2.9 标签多维筛选
- [x] `selectedTags` 状态（Pinia store）
- [x] `allTags` 计算属性（按出现次数降序，最多显示 20 个）
- [x] 标签筛选栏（独立横条，横向滚动）
- [x] AND 逻辑筛选（必须包含所有选中标签）
- [x] 清除筛选按钮（虚线边框，hover 变红色）

#### 2.10 段落复制（Markdown 分段）
- [x] `src/utils/markdown.ts`（`parseMarkdownSections` / `getSectionContent`）
- [x] 复制按钮下拉菜单
  - 复制全文
  - 分隔线
  - 按 `#` / `##` / `###` 标题分段的段落列表
- [x] 缩进层级视觉区分（`paddingLeft: (level-1) * 12px`）

#### 2.11 右键菜单（PromptCard）
- [x] 复制全文
- [x] 编辑
- [x] 收藏 / 取消收藏
- [x] 移动到分类（子菜单，含当前分类排除逻辑）
- [x] 删除
- [x] 移动到分类支持"移除分类"选项

#### 2.12 Popup 快速访问界面优化
- [x] **键盘导航**（↑↓ 方向键切换，↵ 回车复制）
- [x] **搜索框自动聚焦**（打开即聚焦）
- [x] **键盘快捷键提示**（Header: `↑↓` 导航 `↵` 复制 / Footer: `Ctrl+Shift+P` 全局打开）
- [x] **聚焦卡片高亮**（`.popup-card--focused`，背景 + 边框 + 阴影 + 上移）
- [x] **自动滚动到可见区域**（`scrollIntoView` with `block: 'nearest'`）

#### 2.13 构建验证
- [x] `vite build` 成功（`✓ built in 2.57s`）
- [x] 0 lint 错误

---

## 三、文件结构一览

```
prompt-manager-plugin/src/
├── components/          # 12 个 Vue 组件
│   ├── PromptCard.vue        ✅ 右键菜单 + 复制分段 + 批量选择
│   ├── PromptEditor.vue      ✅ 双模式编辑器
│   ├── CategoryDialog.vue    ✅ 分类增删改（含图标/颜色选择器）
│   ├── ContextMenu.vue       ✅ 右键菜单组件
│   ├── ConfirmDialog.vue     ✅ 确认弹窗（危险操作）
│   ├── ImportExport.vue      ✅ 导入导出（含拖拽、预览、策略选择）
│   ├── EmptyState.vue        ✅ 三态空状态（提示词/搜索/分类）
│   ├── TagBadge.vue           ✅ 标签徽章
│   ├── ThemeToggle.vue       ✅ 主题切换
│   ├── ToastNotify.vue       ✅ Toast 通知
│   ├── StatsView.vue         ✅ 统计视图
│   └── HelloWorld.vue        ✅ 占位组件
│
├── stores/              # 3 个 Pinia Store
│   ├── promptStore.ts         ✅ CRUD + 批量 + 标签筛选 + 移动分类
│   ├── categoryStore.ts       ✅ CRUD + 拖拽排序
│   └── settingStore.ts        ✅ 主题模式持久化
│
├── composables/          # 3 个 Composables
│   ├── useSearch.ts           ✅ MiniSearch + 搜索历史 + 高亮片段
│   ├── useToast.ts            ✅ Toast 通知
│   └── useImportExport.ts     ✅ JSON/CSV/Markdown 导出
│
├── db/
│   └── schema.ts              ✅ Dexie v4，预置 5 个分类
│
├── utils/
│   └── markdown.ts            ✅ Markdown 分段解析
│
├── options/
│   ├── App.vue                ✅ 完整管理界面（侧边栏 + 工具栏 + 列表）
│   └── main.ts
│
├── popup/
│   ├── App.vue                ✅ 快速访问界面（键盘导航 + 快捷键）
│   └── main.ts
│
└── types/
    └── index.ts               ✅ 数据模型定义
```

---

## 四、技术亮点

### 4.1 搜索系统
- MiniSearch 增量索引，新增 / 编辑 / 删除单条实时同步，无需全量重建
- 权重差异化（标题 3× > 标签 2× > 描述 1.5× > 内容 1×）
- 搜索历史 localStorage 持久化（关闭页面不丢失）

### 4.2 批量操作
- Pinia Store 支持 `bulkDeletePrompts`
- 搜索索引同步删除（`search.removeFromIndex`）
- 选中态与悬停态联动（CSS 组合选择器）

### 4.3 CSS 架构
- 全局 CSS Variables（`--primary-*`, `--bg-*`, `--text-*`, `--radius-*`, `--shadow-*` 等）
- 暗色模式零 JavaScript 切换（纯 CSS）
- BEM 命名规范（`.batch-bar__*`, `.tag-filter-chip--*` 等）
- 响应式网格布局（`grid-template-columns: repeat(auto-fill, minmax(320px, 1fr))`）

### 4.4 构建问题处理
- 成功解决 PowerShell `\"` 转义导致的 Vue 模板字面量问题
- 成功解决 PowerShell `\n` 转义导致的 CSS 解析器报错问题

---

## 五、待完成功能（Phase 3+）

### Phase 3 — 高级功能（未开始）

- [ ] 二级分类支持（父子层级）
- [ ] 模板变量系统（`{{变量名}}` 填充）
- [ ] Prompt 版本历史
- [ ] 快捷键全局配置
- [ ] 网页内容注入（右键选中文字快速保存）
- [ ] 与 ChatGPT / Claude 页面集成（Content Script 快速插入）
- [ ] 云同步方案（WebDAV / GitHub Gist）
- [ ] 数据统计面板（Top10 最常用、分类分布图）

### 体验优化（可后续迭代）

- [ ] Prompt 卡片视图 / 列表视图切换
- [ ] 字体大小调节（sm / md / lg）
- [ ] 紧凑模式 / 全屏模式切换
- [x] CSV 导出支持 ✅
- [x] Markdown 导出支持 ✅
- [ ] 使用频率排序选项
- [ ] 批量导入预览（JSON 冲突数量提示）

---

## 六、开发规范遵循

| 规范 | 状态 |
|------|------|
| Manifest V3 | ✅ |
| Vue 3 Composition API | ✅ |
| Pinia 状态管理 | ✅ |
| TypeScript 严格类型 | ✅ |
| CSS Variables 暗色模式 | ✅ |
| UnoCSS / Carbon Icons | ✅ |
| vuedraggable 拖拽 | ✅ |
| MiniSearch 全文搜索 | ✅ |
| Dexie.js IndexedDB | ✅ |
| Vite 构建 | ✅ |

---

## 七、关键代码片段

### promptStore — 标签 AND 筛选
```ts
if (selectedTags.value.length > 0) {
  list = list.filter((p) =>
    selectedTags.value.every((tag) => p.tags?.includes(tag)),
  )
}
```

### useSearch — 增量索引
```ts
function updateInIndex(prompt: Prompt) {
  miniSearch.discard(prompt.id)
  addToIndex(prompt)
}
```

### markdown.ts — 段落解析
```ts
export function parseMarkdownSections(content: string): MarkdownSection[] {
  // 按 # 标题拆分，返回 {heading, level, content}
}
```

---

*汇总由 AI 自动生成，最后更新：2026-04-24*
